export const SHADOW_ROOT_SELECTOR = '$';
export const HOST_SELECTOR = ':host';
export const INVALID_SELECTOR = 'invalid selector';
export const DEFAULT_RETRIES = 100;
export const DEFAULT_RETRIES_DELAY = 50;
